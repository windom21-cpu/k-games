# 引き継ぎ書 — スターマイマイ

最終更新: 2026-07-18 / 現行バージョン: v1.0.0

---

## 概要

カタツムリ(マイマイ)を育てて1年間のレースを戦う育成シミュレーション。スターホース風の「育成 → 出走 → 引退 → (将来)繁殖」サイクルを、マイマイ題材でコミカルに翻案したもの。

- サイコロ10個をふって生まれた1頭を、半月きざみの1年間(24ターン)で調教
- 年間12レース(G3→G2→G1)を任意に選んで出走し、賞金をかせぐ
- 最終目標は12月後半の最高峰レース **「マイマイカップ」** 制覇
- 引退した個体は **殿堂** に永久保存され、**v1.1 で繁殖・世代交代に使う前提**の構造になっている

公開URL: https://windom21-cpu.github.io/k-games/star-maimai/
QRコード: `star-maimai/qr.png`

---

## ファイル構成

```
k-games/star-maimai/
├─ index.html   ← ゲーム本体(単一HTML、CSS/JS インライン、約1,760行)
└─ qr.png       ← 公開URLのQRコード
```

外部画像・外部ライブラリなし。カタツムリ・レース場・タイトル虹などすべて Canvas 2D の procedural 描画。

---

## 技術スタック

- 純粋な HTML5 + CSS + JavaScript (バニラ JS、依存ゼロ)
- 描画:
  - 画面切替は DOM ベース(`.screen.active` クラス)+ モーダル(`.modal-back.active`)
  - タイトルアニメ / 厩舎のマイマイ / レース実況 / 引退式は **Canvas 2D**(`drawSnail()` が共通のカタツムリ描画関数。カラの値で殻が大きく・キラキラになる)
- 入力: click / タッチ(`touch-action: manipulation`)
- 効果音: Web Audio API オシレータ(`SFX` オブジェクト)。🔊/🔇 トグルは `localStorage["starMaimai.mute"]` に永続化
- 永続化: `localStorage["starMaimai.slot0..2"]`(下記セーブ仕様)
- ビルド工程なし。`index.html` を編集して push するだけで反映 (1〜2分)

---

## ゲーム仕様

### 画面遷移

```
タイトル(虹+マイマイ行進アニメ)
  ↓ [はじめる]
セーブスロット選択(3スロット、削除ボタン付き)
  ↓
(current がいなければ)サイコロ画面 → 名まえ入力
  ↓
メイン(厩舎): エサ選択+行動 → ターン消費
  ├─ 📅 レースカレンダー(モーダル)
  ├─ 🏁 出走登録(モーダル: 天候・地面・対戦相手の見立て)→ レース実況 → 結果
  ├─ 🏆 殿堂
  └─ 引退させる…(確認モーダル)
  ↓ (24ターン終了 or 途中引退)
引退式(花ふぶき演出+戦績サマリー)→ 殿堂入り → 新しいマイマイへ(年数+1)
```

### マイマイの能力(4ステ+隠し)

| 表示 | 意味 | 上限 | 育て方 |
|---|---|---|---|
| ぬめり | 速さ(基本走力) | 120 | 調教「ガラス板すべり」 |
| 体力 | スタミナ。長距離でバテにくい | 120 | 調教「ながい散歩」 |
| 根性 | 残り24%のスパート+バテ後のねばり | 120 | 調教「タカとにらめっこ」 |
| カラ | カラカラ(乾いた)地面への耐性 | 120 | **調教では伸びない。カルシウムのエサのみ** |

ランク表示: F(<22) E D C B A S(≥110)。

**隠しステータス(画面に出さない)**:
- `hidden.gf` — 成長係数。サイコロ合計(10〜60)から 0.90〜1.25
- `hidden.apt.{num,sta,gut,shl}` — **調教効果**。ステ別に gauss(1.0, 0.25) を 0.6〜1.7 にクランプ。調教の伸び = 基本値 × apt × gf × 疲労補正。**サイコロが悪くても apt が高ければ大化けする**(ユーザー要望)
- ヒントは (1) 誕生時のコーチコメント(apt 平均×gf の水準で5段階の台詞)、(2) 調教時の「ものすごい伸びだ!」等の実感コメントのみ

### 誕生(サイコロ10個)

- 青3個→ぬめり(合計×3.0)、緑3個→体力(×3.0)、橙2個→根性(×3.5)、紫2個→カラ(×3.5)
- 合計値が成長係数 gf にも影響(=良い目は初期値と成長の二重取り)
- 名まえは手入力(最大8文字)or「おまかせ名まえ」生成

### ターン(半月×24)

毎ターン、**エサを選んで行動1つ**:
- エサ: ふつう(無料) / カルシウム(200G, カラ+2) / 特選カルシウム(500G, カラ+4・体力+1)。資金不足時は自動でふつうに降格
- 行動: 調教3種 / 休養(つかれ-50, 調子上向き) / レース出走(開催ターンのみ)
- つかれ: 調教+17前後、レース+22、80以上で調教失敗35%。55以上でレースの速度ペナルティ
- 調子: 5段階(ぜつ不調〜ぜっ好調)。毎ターン確率でゆらぐ。レース速度 ×0.90〜×1.09

### レース(年12戦)

| 時期 | レース | 格 | きょり | 雨傾向 |
|---|---|---|---|---|
| 2月前半 | 新芽ステークス | G3 | 50cm | — |
| 3月前半 | はるさめ特別 | G3 | 100cm | やや雨 |
| 4月前半 | さくらカップ | G2 | 100cm | — |
| 5月前半 | 若葉ステークス | G3 | 150cm | — |
| 6月前半 | あじさい賞 | G2 | 100cm | 雨多い |
| 6月後半 | 梅雨グランプリ | G1 | 150cm | ほぼ雨 |
| 7月後半 | 真夏スプリント | G2 | 50cm | ほぼ晴れ |
| 8月後半 | 流星特別 | G3 | 100cm | 晴れ寄り |
| 9月後半 | お月見カップ | G2 | 150cm | — |
| 10月後半 | 紅葉ステークス | G1 | 100cm | — |
| 11月後半 | しぐれ記念 | G2 | 50cm | やや雨 |
| 12月後半 | **マイマイカップ** | MC | 150cm | — |

- 出走料/賞金(1〜3着): G3 100G / 2000-800-400、G2 300G / 5000-2000-1000、G1 500G / 12000-5000-2500、MC 1000G / 30000-12000-6000
- 出走条件: G3=自由 / G2=1勝 or 賞金1500G / G1=G2以上3着内 or 賞金6000G / MC=G1で3着内 or 賞金15000G
- 8頭立て(自分+NPC7)。NPC は `GRADE_NPC` の中心値±gauss で生成、カラだけ分散大きめ(乾き地面で明暗が出る)

### 天候・地面(マイマイの本領)

月別の雨確率(6月高・7〜8月低)+ レース毎の `wetBias` で抽選:
- **ぬれぬれ**(☔): 全員 ×1.09。カラ無関係 → 実力勝負・番狂わせの舞台
- **しっとり**(☁): ほぼ等倍
- **カラカラ**(☀): 速度 ×0.76〜1.00(カラ0→120で線形)+ スタミナ消費 ×1.3(カラで軽減)。**カラを鍛えた個体だけが晴れでも強い**(ユーザー要望どおり)

### レースシミュレーション

`stepRace()` を dt きざみで回す物理風モデル(スキップ時は `fastForwardRace()` で 0.1s きざみ一括計算 — 実況と同一ロジック):
- 速度 = (2.1 + ぬめり×0.03) × 地面係数 × 調子係数 × 疲労係数 × ゆらぎノイズ
- スタミナ = 55 + 体力×2.1。毎秒 6.5 消費(カラカラで増、スパート中×1.35)。切れると速度×(0.52+根性×0.0013)=バテ
- 残り24%でスパート: ×(1 + 根性×0.002×乱数)
- スタートに 0〜0.7s の出遅れ乱数
- 実況: ×1/×4倍速トグル、結果へスキップ、ぬめり跡・バテ表示・🥇🥈🥉

### 引退・殿堂

- 24ターン経過で自動引退。途中引退も可(確認モーダル、取消不可)
- 殿堂エントリに `canBreed: true` を必ず付与 — **引退個体は一生繁殖可能**(ユーザー方針)。`hidden`(gf/apt)もスナップショット保存済みなので、v1.1 の遺伝計算にそのまま使える
- 資金と殿堂はスロット内で世代をまたいで引き継ぎ。引退→即、次のサイコロへ(年数+1)

---

## セーブ仕様(重要)

`localStorage["starMaimai.slot0" | "slot1" | "slot2"]` に JSON:

```
{ version: 1,            ← SAVE_VERSION
  money, year, hall: [殿堂エントリ...], current: マイマイ|null, updatedAt }
```

**マイグレーション**: `MIGRATIONS = { 旧version: (data)=>新data }` のチェーン方式。`loadSlot()` が読み込み時に `migrateSave()` を通し、変換後は即再保存する。

> ⚠ **セーブ構造を変えるときの手順**: (1) `SAVE_VERSION` を +1、(2) `MIGRATIONS[旧番号]` に変換関数を追加。これを守れば公開後のアップデートでプレイヤーのデータが消えない。v1.1(繁殖)ではここを必ず使うこと。

---

## バランス設計と検証

調整パラメータは **`TUNE` オブジェクトに全集約**(初期値倍率・成長・疲労・エサ・レース物理・NPC強度以外に `GRADE_NPC` / `GRADES` / `RACES` / `MONTH_WET`)。

v1.0.0 決定値は、実コードをヘッドレスで 500シーズン×3方針 自動プレイして調整した:

| プレイスタイル | G3勝率 | G2勝率 | G1勝率 | MC制覇/シーズン |
|---|---|---|---|---|
| 雑(ランダム調教・カルシウムなし) | 63% | 29% | 5% | 1.4% |
| 丁寧(最低ステ調教+カルシウム) | 65% | 39% | 9% | 3.6% |
| 上級(ぬめり体力集中+カルシウム) | 73% | 55% | 22% | 19% |

設計意図 =「初代でも運が良ければ勝てる。うまく育てれば数世代で届く」(ユーザー指定)。

テストハーネスはセッションの scratchpad にあった `simtest.js`(DOM スタブ+vm で index.html の `<script>` を実行し、`doTrain`/`setupRace`/`applyRaceResult` など実関数で回す方式)。再調整時は同方式を再作成すると早い。

---

## v1.1 予定(繁殖・世代交代)— 設計メモ

- 殿堂の `canBreed: true` 個体から2頭(または1頭+野生)を選んで配合
- 遺伝方針(ユーザー指定): 基本は「強い×強い→強い」のシンプル型。ただし**弱い親から強い子が生まれる余地を残す**(変異幅を広めに)。`hidden.apt` / `gf` も遺伝対象にできるよう保存済み
- サイコロ演出は継続し「配合補正+サイコロ」のハイブリッドを想定
- セーブは `SAVE_VERSION: 2` + `MIGRATIONS[1]` で拡張する

---

## バージョン管理

セマンティックバージョニング(`X.Y.Z`)。`index.html` 冒頭の `GAME_VERSION` 定数とこの履歴を同期。

### バージョン履歴

- v1.0.0 (2026-07-18) — 初版。育成+レース+1年+引退まで(段階リリースの第1弾)。セーブ3スロット+マイグレーション、隠し調教効果、殿堂(繁殖フラグ付き)

---

## 開発フロー

```bash
# 1. 編集
$EDITOR /home/sk/デスクトップ/k-games/star-maimai/index.html

# 2. ローカル確認
xdg-open /home/sk/デスクトップ/k-games/star-maimai/index.html

# 3. コミット & push
cd /home/sk/デスクトップ/k-games
git add star-maimai/index.html
git -c user.name='windom21-cpu' -c user.email='sk21.lawyer@gmail.com' \
  commit -m "..."
git push origin main
```

QRコード再生成 (URL が変わった時のみ):

```bash
qrencode -o /home/sk/デスクトップ/k-games/star-maimai/qr.png -s 10 -m 2 \
  "https://windom21-cpu.github.io/k-games/star-maimai/"
```

---

## チューニング箇所早見表(`TUNE` ほか)

| 触る場所 | 現在値 | 意味 |
|---|---|---|
| `trainBaseMin/Max` | 8〜13 | 調教1回の基本伸び(×apt×gf×疲労補正) |
| `gfBase/gfPerPip` | 0.90 / 0.007 | サイコロ合計→成長係数 |
| `aptMean/Sd/Min/Max` | 1.0 / 0.25 / 0.6 / 1.7 | 隠し調教効果の分布 |
| `trainFatigue / restRecover / raceFatigue` | 17 / 50 / 22 | 疲労の出入り |
| `failFatigue / failChance` | 80 / 0.35 | 疲労時の調教失敗 |
| `feeds` | 0/200/500G | エサ3種のコストと効果 |
| `baseV / vPerNum` | 2.1 / 0.030 | レース基本速度 (cm/s) |
| `stBase / stPerSta / drainPerSec` | 55 / 2.1 / 6.5 | スタミナ量と消費 |
| `wetSpeedMul / drySpeedMin / dryDrainMul` | 1.09 / 0.76 / 1.3 | 地面補正(カラの効き) |
| `spurtZone / spurtPerGut / bateBase` | 0.24 / 0.002 / 0.52 | スパートとバテ |
| `GRADE_NPC` | G3:30 G2:48 G1:62 MC:76 | NPC 強さ中心値(難易度の本体) |
| `GRADES` | — | 出走料・賞金 |
| `MONTH_WET` + `RACES[].wetBias` | — | 月別降雨と各レースの雨傾向 |
| `startMoney` | 1500 | 初期資金 |
